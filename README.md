Bash/zsh profile configuration for fast machine switching.

### Basic Installation

Install the bash/zsh profile setup via curl.

#### via curl

```shell
bash -c "$(curl -fsSL https://raw.githubusercontent.com/nxtstep/dotfiles/master/tools/install.sh)"
```
